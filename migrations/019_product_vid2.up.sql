CREATE TABLE IF NOT EXISTS product_vid2 (
 id BIGINT GENERATED BY DEFAULT AS IDENTITY  PRIMARY KEY,
 id_1c TEXT NOT NULL UNIQUE,
 name_1c TEXT NOT NULL,
 registrator_id BIGINT NOT NULL REFERENCES registrator(id),

 changed_date TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP, 
 create_date TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
);

ALTER TABLE "product"
  ADD COLUMN IF NOT EXISTS product_vid2_id BIGINT NOTREFERENCES product_vid2(id);

COMMENT ON COLUMN product.product_vid2_id IS 'id реквизита ВидНоменклатуры';